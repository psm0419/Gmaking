<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.gmaking.conversation.dao.ConversationDAO">
    <select id="findOpen" resultMap="mqp">
        SELECT conversation_id, user_id, character_id, created_date
        FROM tb_conversation
        WHERE user_id = #{userId}
            AND character_id = #{characterId}
        ORDER BY created_date DESC
        LIMIT 1
    </select>

    <insert id="create" useGeneratedKeys="true" keyProperty="conversationId">
        INSERT INTO tb_conversation
            (user_id, character_id, created_date, created_by, updated_date, updated_by)
        VALUES (#{userId}, #{characterId}, NOW(), #{actor}, NOW(), #{actor});
    </insert>
</mapper>