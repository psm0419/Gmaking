<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.gmaking.conversation.dao.ConversationDAO">
    <insert id="insert" parameterType="com.project.gmaking.conversation.vo.ConversationVO"
            useGeneratedKeys="true" keyProperty="conversationId">
        INSERT INTO tb_conversation (user_id, character_id)
        VALUES (#{userId}, #{characterId})
    </insert>

    <select id="findById" resultType="com.project.gmaking.conversation.vo.ConversationVO">
        SELECT conversation_id AS conversationId,
        user_id AS userId,
        character_id AS characterId,
        create_at AS createAt,
        updated_at AS updatedAt
        FROM tb_conversation
        WHERE conversation_id = #{id}
    </select>
</mapper>