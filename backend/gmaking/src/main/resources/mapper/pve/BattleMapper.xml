<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.gmaking.pve.dao.BattleDAO">

    <update id="updateCharacterStageClear" parameterType="string">
        UPDATE TB_CHARACTER
        SET TOTAL_STAGE_CLEARS = TOTAL_STAGE_CLEARS + 1,
        UPDATED_DATE = NOW()
        WHERE CHARACTER_ID = #{characterId};
    </update>

    <insert id="insertBattleLog" parameterType="com.project.gmaking.pve.vo.BattleLogVO">
        INSERT INTO TB_BATTLE_LOG
        (BATTLE_ID, CHARACTER_ID, BATTLE_TYPE, OPPONENT_ID, IS_WIN, TURN_COUNT, CREATED_BY)
        VALUES
        (#{battleId}, #{characterId}, #{battleType}, #{opponentId}, #{isWin}, #{turnCount}, #{createdBy})
    </insert>

</mapper>
