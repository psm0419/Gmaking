<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.gmaking.login.dao.LoginDAO">

    <resultMap id="loginResultMap" type="com.project.gmaking.login.vo.LoginVO">
        <id column="USER_ID" property="userId"/>
        <result column="USER_NAME" property="userName"/>
        <result column="USER_EMAIL" property="userEmail"/>
        <result column="IS_EMAIL_VERIFIED" property="isEmailVerified"/>
        <result column="USER_PASSWORD" property="userPassword"/>
        <result column="USER_NICKNAME" property="userNickname"/>
<!--        <result column="IMAGE_ID" property="imageId"/>-->
        <result column="CREATED_DATE" property="createdDate"/>
        <result column="ROLE" property="role"/>

        <result column="IS_CHARACTER_EXISTS" property="hasCharacter"/>
        <result column="CHARACTER_IMG_URL" property="characterImageUrl"/>
    </resultMap>

    <select id="selectUserById" resultMap="loginResultMap">
        SELECT
<<<<<<< HEAD
            USER_ID,
            USER_NAME,
            USER_EMAIL,
            IS_EMAIL_VERIFIED,
            USER_PASSWORD,
            USER_NICKNAME,
<!--            IMAGE_ID,-->
            ROLE,
            CREATED_DATE
        FROM TB_USER
        WHERE USER_ID = #{userId}
=======
            T1.USER_ID,
            T1.USER_NAME,
            T1.USER_EMAIL,
            T1.IS_EMAIL_VERIFIED,
            T1.USER_PASSWORD,
            T1.USER_NICKNAME,
            T1.IMAGE_ID,
            T1.ROLE,
            T1.CREATED_DATE,
            CASE WHEN T2.USER_ID IS NOT NULL THEN TRUE ELSE FALSE END AS IS_CHARACTER_EXISTS,
            T3.IMAGE_URL AS CHARACTER_IMG_URL
        FROM TB_USER T1
                 LEFT JOIN TB_CHARACTER T2 ON T1.USER_ID = T2.USER_ID
                 LEFT JOIN TB_IMAGE T3 ON T2.IMAGE_ID = T3.IMAGE_ID
        WHERE T1.USER_ID = #{userId}
>>>>>>> 70cb8c38e5c902f714ca72978649163789b6013a
    </select>

    <insert id="register" parameterType="com.project.gmaking.login.vo.RegisterRequestVO">
        INSERT INTO TB_USER (
            USER_ID,
            USER_NAME,
            USER_EMAIL,
            IS_EMAIL_VERIFIED,
            USER_PASSWORD,
            USER_NICKNAME,
            ROLE
        ) VALUES (
            #{userId},
            #{userName},
            #{userEmail},
            'N',
            #{userPassword},
            #{userNickname},
             'USER'
        )
    </insert>

    <select id="checkDuplicate" resultType="int">
        SELECT
            COUNT(*)
        FROM TB_USER
        WHERE
        <choose>
            <when test="type == 'userId'">USER_ID = #{value}</when>
            <when test="type == 'userNickname'">USER_NICKNAME = #{value}</when>
            <when test="type == 'userEmail'">USER_EMAIL = #{value}</when>
            <otherwise>1 = 0</otherwise>
        </choose>
    </select>

    <delete id="deleteUser">
        DELETE
        FROM TB_USER
        WHERE USER_ID = #{userId}
    </delete>

    <select id="findUserIdByNameAndEmail" resultType="string">
        SELECT USER_ID
        FROM TB_USER
        WHERE USER_NAME = #{userName} AND USER_EMAIL = #{userEmail}
    </select>

    <update id="updatePassword">
        UPDATE TB_USER
        SET USER_PASSWORD = #{newHashedPassword}
        WHERE USER_ID = #{userId}
    </update>

    <select id="selectUserBySocialId" resultMap="loginResultMap">
        SELECT
            T1.USER_ID,
            T1.USER_NAME,
            T1.USER_EMAIL,
            T1.IS_EMAIL_VERIFIED,
            T1.USER_PASSWORD,
            T1.USER_NICKNAME,
            T1.IMAGE_ID,
            T1.ROLE,
            T1.CREATED_DATE,
            CASE WHEN T2.USER_ID IS NOT NULL THEN TRUE ELSE FALSE END AS IS_CHARACTER_EXISTS,
            T3.IMAGE_URL AS CHARACTER_IMG_URL
        FROM TB_USER T1
                 LEFT JOIN TB_CHARACTER T2 ON T1.USER_ID = T2.USER_ID
                 LEFT JOIN TB_IMAGE T3 ON T2.IMAGE_ID = T3.IMAGE_ID
        WHERE T1.USER_ID = #{userId}
    </select>

    <insert id="insertSocialUser" parameterType="com.project.gmaking.login.vo.LoginVO">
        INSERT INTO TB_USER (
            USER_ID,
            USER_NAME,
            USER_EMAIL,
            IS_EMAIL_VERIFIED,
            USER_PASSWORD,
            USER_NICKNAME,
            ROLE
        ) VALUES (
            #{userId},
            #{userName},
            #{userEmail},
            'Y',
            #{userPassword},
            #{userNickname},
            'USER'
        )
    </insert>

    <select id="selectUserByEmail" resultMap="loginResultMap">
        SELECT
            USER_ID,
            USER_NAME,
            USER_EMAIL,
            IS_EMAIL_VERIFIED,
            USER_PASSWORD,
            USER_NICKNAME,
<!--            IMAGE_ID,-->
            ROLE,
            CREATED_DATE
        FROM TB_USER
        WHERE USER_EMAIL = #{userEmail}
    </select>

    <update id="updateUserCharacterInfo">
        UPDATE TB_USER
        SET
            HAS_CHARACTER = TRUE,
            CHARACTER_IMAGE_URL = #{imageUrl}
        WHERE USER_ID = #{userId}
    </update>

</mapper>